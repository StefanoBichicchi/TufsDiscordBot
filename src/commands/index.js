const help = require('./help');
const DiceRoller = require('./diceroller');
const InitTracker = require('./initTracker');
const Ping = require('./ping');
const LutMeme = require('./lutmeme');
const DnDMeme = require('./dndmeme');

module.exports = {
	help,
	DiceRoller,
	InitTracker,
	Ping,
	LutMeme,
	DnDMeme
};